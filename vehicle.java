import java.util.*;
class vehicle
{
	public static void main(String args[])
	{
		Scanner sc=new Scanner(System.in);
		int n=sc.nextInt();
		String s=sc.nextLine();
		len=s.charAt((s.length()-1)-str.ch(0)+1)
		pow(len,2);
		int r=sc.nextInt();
	}
}